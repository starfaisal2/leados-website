// This route is intentionally empty — /auto-seo is served via a Next.js rewrite
// in next.config.ts that maps /auto-seo → /auto-seo.html (public/ directory).
// The rewrite runs before the page router so this file is never reached.
export default function AutoSeoPage() {
  return null;
}
